#include <stdio.h>

int main() {
    int number = 127;

    char alpha = 'c';

    float weight = 90.4;
    double height = 183.4;

    printf("숫자 : %d \n", number);
    printf("알파벳 : %c \n", alpha);
    printf("체중 : %f \n", weight);
    printf("키 : %f \n", height);
    printf("문자열 : %d", "C 언어 배우기");

    return 0;
}